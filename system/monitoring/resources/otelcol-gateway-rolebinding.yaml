apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding

metadata:
  name: otelcol-gateway-rolebinding

roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: otelcol-gateway-role
subjects:
  - kind: ServiceAccount
    namespace: monitoring
    name: otelcol-gateway-sa
