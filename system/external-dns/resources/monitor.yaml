apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: external-dns
  namespace: external-dns

spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: external-dns
  endpoints:
    - port: http
