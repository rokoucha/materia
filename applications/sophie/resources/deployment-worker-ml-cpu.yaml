apiVersion: apps/v1
kind: Deployment

metadata:
  name: worker-ml-cpu
  annotations:
    argocd.argoproj.io/sync-wave: "2"

spec:
  # #206 supplies feature queueing before this profile is enabled.
  replicas: 0
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: sophie-worker-ml-cpu
  template:
    metadata:
      labels:
        app: sophie-worker-ml-cpu
    spec:
      terminationGracePeriodSeconds: 300
      imagePullSecrets:
        - name: ghcr-credentials
      containers:
        - name: worker
          image: ghcr.io/rokoucha/sophie-worker-ml:20260918.160359
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: postgresql-app
                  key: uri
            - name: ML_EXECUTION_PROFILE
              value: cpu
            - name: OTEL_SERVICE_NAME
              value: sophie/worker-ml-cpu
            - name: SOPHIE_ROLE
              value: worker-ml
          envFrom:
            - configMapRef:
                name: config
            - secretRef:
                name: sophie-secrets
          ports:
            - name: health
              containerPort: 3000
          readinessProbe:
            httpGet:
              path: /api/health/ready
              port: health
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health/live
              port: health
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              cpu: 500m
              ephemeral-storage: 1Gi
              memory: 512Mi
            limits:
              cpu: "2"
              ephemeral-storage: 4Gi
              memory: 2Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
          volumeMounts:
            - name: tmp
              mountPath: /tmp
        - name: model-callee
          image: ghcr.io/rokoucha/sophie-worker-ml:20260918.160359
          command:
            - python3
            - /opt/sophie-ml/image_feature_callee.py
          env:
            - name: ML_EXECUTION_PROFILE
              value: cpu
            - name: ML_THREADS
              value: "2"
          resources:
            requests:
              cpu: "1"
              ephemeral-storage: 1Gi
              memory: 3Gi
            limits:
              cpu: "2"
              ephemeral-storage: 2Gi
              memory: 4Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: models
              mountPath: /opt/sophie-models
              readOnly: true
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: ml-models
        - name: tmp
          emptyDir:
            sizeLimit: 4Gi
