apiVersion: apps/v1
kind: Deployment

metadata:
  name: worker-inference-gpu
  annotations:
    argocd.argoproj.io/sync-wave: "2"

spec:
  replicas: 1
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: sophie-worker-inference-gpu
  template:
    metadata:
      labels:
        app: sophie-worker-inference-gpu
    spec:
      nodeSelector:
        kubernetes.io/hostname: phosphorus
      terminationGracePeriodSeconds: 300
      imagePullSecrets:
        - name: ghcr-credentials
      containers:
        - name: worker
          image: ghcr.io/rokoucha/sophie-worker-inference:20260917.211147
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: postgresql-app
                  key: uri
            - name: INFERENCE_EXECUTION_PROFILE
              value: gpu
            - name: OTEL_SERVICE_NAME
              value: sophie/worker-inference-gpu
            - name: SOPHIE_ROLE
              value: worker-inference
          envFrom:
            - configMapRef:
                name: config
            - secretRef:
                name: sophie-secrets
          ports:
            - name: health
              containerPort: 3000
          readinessProbe:
            httpGet:
              path: /api/health/ready
              port: health
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health/live
              port: health
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              cpu: 250m
              ephemeral-storage: 512Mi
              memory: 512Mi
            limits:
              cpu: "1"
              ephemeral-storage: 1Gi
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
          volumeMounts:
            - name: tmp
              mountPath: /tmp
        - name: model-callee
          image: ghcr.io/rokoucha/sophie-worker-inference:20260917.211147
          command:
            - python3
            - /opt/sophie-inference/image_feature_callee.py
          env:
            - name: INFERENCE_EXECUTION_PROFILE
              value: gpu
            - name: INFERENCE_THREADS
              value: "2"
          resources:
            requests:
              cpu: "1"
              ephemeral-storage: 512Mi
              gpu.intel.com/i915: "1"
              memory: 3Gi
            limits:
              cpu: "2"
              ephemeral-storage: 1Gi
              gpu.intel.com/i915: "1"
              memory: 4Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 2Gi
