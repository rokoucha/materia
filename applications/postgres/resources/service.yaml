apiVersion: v1
kind: Service

metadata:
  name: service

spec:
  selector:
    app: default
  type: NodePort
  ipFamilies:
    - IPv4
    - IPv6
  ipFamilyPolicy: RequireDualStack
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      nodePort: 5432
