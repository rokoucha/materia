apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: redis

spec:
  selector:
    matchLabels:
      redis_setup_type: standalone
  jobLabel: mastodon
  endpoints:
    - port: redis-exporter
      interval: 30s
      scrapeTimeout: 10s
