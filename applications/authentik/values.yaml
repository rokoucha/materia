authentik:
  secret_key: "PleaseGenerateA50CharKey"
  error_reporting:
    enabled: false
  postgresql:
    password: "ThisIsNotASecurePassword"

server:
  ingress:
    enabled: true
    hosts:
      - authentik.domain.tld

redis:
  enabled: true
  master:
    persistence:
      enabled: false
